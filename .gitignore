# vscode
.vscode 

# Intellij
*.iml
.idea

# npm
node_modules

# Don't include the compiled server bundle in the repo.
# It is produced by `npm run build` and included in the .mcpb release artifact.
main.js
server/index.js

# Packed MCPB bundle — produced by `npm run pack` / `mcpb pack`.
*.mcpb

# Exclude sourcemaps
*.map

# obsidian
data.json

# Exclude macOS Finder (System Explorer) View States
.DS_Store
